spring:
  application:
    name: contact-app
  datasource:
    url: jdbc:h2:mem:contact-app-db
    username: admin
    password: admin
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
logging:
  level:
    org:
      jooq:
        tools:
        #LoggerListener: debug